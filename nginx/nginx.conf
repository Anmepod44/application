# nginx.conf
server {
    listen 80;  # Default HTTP port

    location / {
        proxy_pass http://94.130.25.34:8000;  # Forward requests to the desired IP and port
        proxy_set_header Host $host;          # Preserve the original Host header
    }
}


# HTTPS configuration (optional, uncomment and configure if needed)
# server {
#     listen 443 ssl;
#     server_name 94.130.25.34;  # Replace with your server's IP or domain name
#
#     # SSL certificate configuration
#     ssl_certificate /etc/nginx/ssl/certificate.crt;       # Path to your SSL certificate
#     ssl_certificate_key /etc/nginx/ssl/private.key;       # Path to your SSL private key
#
#     # SSL settings
#     ssl_protocols TLSv1.2 TLSv1.3;                        # Enable secure TLS protocols
#     ssl_ciphers HIGH:!aNULL:!MD5;                         # Use strong ciphers
#     ssl_prefer_server_ciphers on;                         # Prefer server ciphers
#     ssl_session_cache shared:SSL:10m;                     # SSL session cache
#     ssl_session_timeout 1d;                               # SSL session timeout
#     ssl_session_tickets off;                              # Disable session tickets
#
#     location / {
#         proxy_pass http://94.130.25.34:8000;              # Forward requests to backend
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;          # Pass client IP
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; # Forward headers
#         proxy_set_header X-Forwarded-Proto $scheme;       # Pass protocol (http/https)
#     }
#
#     # Optional: Add headers for security
#     # add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     # add_header X-Content-Type-Options nosniff;
#     # add_header X-Frame-Options DENY;
#     # add_header X-XSS-Protection "1; mode=block";
# }
